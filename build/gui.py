# This file was generated by the Tkinter Designer by Parth Jadhav
# https://github.com/ParthJadhav/Tkinter-Designer

import sys, os
from pathlib import Path

# from tkinter import *
# Explicit imports to satisfy Flake8
from tkinter import Tk, Canvas, Entry, Text, Button, PhotoImage


OUTPUT_PATH = Path(__file__).parent

def resource_path(relative_path):
    """Get absolute path to resource, works for dev and for PyInstaller"""
    try:
        # PyInstaller creates a temp folder and stores path in _MEIPASS
        base_path = sys._MEIPASS
    except Exception:
        base_path = os.path.abspath(".")
    
    return os.path.join(base_path, relative_path)

def relative_to_assets(path: str) -> Path:
    return Path(resource_path(f"resources/assets/frame0/{path}"))


window = Tk()

window.geometry("1270x790")
window.configure(bg = "#FCECB7")


canvas = Canvas(
    window,
    bg = "#FCECB7",
    height = 790,
    width = 1270,
    bd = 0,
    highlightthickness = 0,
    relief = "ridge"
)

canvas.place(x = 0, y = 0)
canvas.create_rectangle(
    0.0,
    0.0,
    1270.0,
    98.0,
    fill="#792D1B",
    outline="")

canvas.create_text(
    128.0,
    24.0,
    anchor="nw",
    text="PAMBAYANG DALUBHAASAAN NG MARILAO",
    fill="#FFDA0C",
    font=("Inter SemiBold", 24 * -1)
)

canvas.create_text(
    128.0,
    47.0,
    anchor="nw",
    text="\"Where quality education is a right, not a privilage\"",
    fill="#FFDA0C",
    font=("Inter SemiBold", 14 * -1)
)

image_image_1 = PhotoImage(
    file=relative_to_assets("image_1.png"))
image_1 = canvas.create_image(
    79.0,
    49.0,
    image=image_image_1
)

canvas.create_rectangle(
    0.0,
    98.0,
    1270.0,
    140.0,
    fill="#FFDA0C",
    outline="")

# Create text buttons for navigation
button_home = Button(
    window,
    text="Home",
    font=("Inter", 16),
    bg="#FFDA0C",
    fg="#792D1B",
    relief="flat",
    cursor="hand2"
)
button_home.place(x=439.0, y=108.0, width=80, height=24)

button_programs = Button(
    window,
    text="Programs",
    font=("Inter", 16),
    bg="#FFDA0C",
    fg="#792D1B",
    relief="flat",
    cursor="hand2"
)
button_programs.place(x=532.0, y=108.0, width=100, height=24)

button_enrollment = Button(
    window,
    text="Enrollment",
    font=("Inter", 16),
    bg="#FFDA0C",
    fg="#792D1B",
    relief="flat",
    cursor="hand2"
)
button_enrollment.place(x=653.0, y=108.0, width=110, height=24)

button_profile = Button(
    window,
    text="Profile",
    font=("Inter", 16),
    bg="#FFDA0C",
    fg="#792D1B",
    relief="flat",
    cursor="hand2"
)
button_profile.place(x=782.0, y=108.0, width=80, height=24)

image_image_2 = PhotoImage(
    file=relative_to_assets("image_2.png"))
image_2 = canvas.create_image(
    635.0,
    465.0,
    image=image_image_2
)
window.resizable(False, False)
window.mainloop()